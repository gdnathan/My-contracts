tips:

-if you already have a mint token, put it's address in .env
-scripts "execute swap" and "execute swap erc20" can only be 
-when initiating erc swap, if you run into "TokenAccountNotFoundError", run the script again until you don't have the error anymore
